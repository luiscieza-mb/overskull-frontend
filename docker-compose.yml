services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:8000/api}
    container_name: overskull-frontend
    restart: unless-stopped
    ports:
      - "5173:80"
